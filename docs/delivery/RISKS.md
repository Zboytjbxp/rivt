# Gate A Risk Register

| ID | Severity | Risk | Current evidence | Required mitigation / exit |
|---|---:|---|---|---|
| R-001 | Critical | Authentication fails open in the browser | Closed: browser fallback removed; CI and live failed-login checks pass on deployed commit | Preserve fail-closed coverage in every auth packet |
| R-002 | Critical | Private records are session-cookie scoped without authenticated ownership | Mitigated and deployed for legacy routes with CI cross-user isolation and live 401 checks | Normalize domain ownership in Packets 01-07 |
| R-003 | Critical | Seed jobs/talent/reviews can appear as real marketplace activity | Mitigated further in Packet 08 production: live hardening audit found two public test profiles and twelve active smoke organizations, guarded cleanup made the profiles private and closed the organizations without deletion, and the final live audit reported zero seed/demo findings, zero public network profiles, and zero open jobs. Dead legacy fixtures and 115 legacy app-state rows remain quarantined for rollback/retention only; the app-state bridge is retired in deployed source `00147c8`. The 2026-07-03 minimal truthfulness pass also removed fake-looking Shop Talk fallback authors, thumbnails, timestamps, votes, and comment counts; remaining starter prompts are labeled as RIVT prompts with zero votes. The 2026-07-04 launch-QA cleanup removes fabricated fresh-account record/training progress and prevents authenticated Shop Talk posts from fake-succeeding locally when server persistence fails. | Synthetic data confined to tests/demo environment; legacy app-state rows remain non-user-facing and no bridge can hydrate or mutate them |
| R-004 | High | Google users receive fabricated role/company/location | Packet 02 OIDC identity linking and pending role-correct onboarding pass CI without fabricated profile data | Verify live OAuth onboarding and existing-account preservation after Packet 02 deploy |
| R-005 | High | Session fixation/overlong sessions | Rotation, device/session listing, single-session revocation, and revoke-all-other-sessions pass Packet 02 CI | Deploy Packet 02 and verify device/session controls live |
| R-006 | High | Core records can conflict or be overwritten as one JSON blob | Mitigated in production for jobs in Packet 03, applications/offers/active work in Packet 04, conversations/messages/notifications in Packet 05, project records/completion in Packet 06, and reviews/safety/support/admin restrictions in Packet 07. The legacy app-state hydrate/save loop and authenticated app-state/event/payment-export bridge are retired in deployed source `00147c8`; live hardening smoke confirms anonymous fail-closed behavior and zero seed findings. | Preserve integration coverage for authenticated retired responses and keep legacy rows quarantined until restore/retention signoff |
| R-007 | High | Upload authorization and type safety are insufficient | Mitigated for project evidence in Packet 06: accepted-work participant authorization, content-signature rejection, SHA-256, private object keys, per-file status, signed access, and live smoke passed. Legacy/profile/portfolio upload surfaces still need follow-up before broad launch. | Wire remaining media surfaces to project/profile ownership and decide malware-scanning provider before scale |
| R-008 | High | Known upload denial-of-service vulnerability | Closed in source and CI: Multer 2.2.0 and zero-vulnerability audit | Verify deployed lockfile/build |
| R-009 | High | No automated authorization or critical-flow tests | Packet 02 CI covers disposable-DB migrations, account lifecycle/onboarding, cross-user authorization, and fail-closed browser auth | Expand critical journeys by packet and add live provider acceptance evidence |
| R-010 | High | Production revision cannot be proven | Closed and preserved through Packet 08: health/readiness report exact source `aac69811ef6d9b4088e5f4c95bc4bc3886a904ce`; deployment ledger records Railway deployment/readiness evidence, and `EXPECTED_SOURCE_COMMIT=aac69811ef6d9b4088e5f4c95bc4bc3886a904ce npm run monitor:production` passed. | Require source SHA for every deployment |
| R-011 | High | Startup schema mutation can partially fail or drift | Closed: checksummed transactional migrations, advisory lock, CI rollback, and production ledger/readiness are verified | Preserve immutable migration history and release checks |
| R-012 | High | CORS/CSRF/rate-limit posture is not launch safe | Improved in Packet 08: approved origins, SameSite cookies, domain quotas, operational kill switches, and PostgreSQL-backed durable auth/write/upload rate limits are live on migration `0009_durable_rate_limits`. Remaining risk is the broader CSRF/enumeration threat review and threshold tuning under real pilot traffic. | Complete CSRF/enumeration threat review and monitor/tune limits during named-cohort launch |
| R-013 | Medium | Lint debt hides dead behavior and unsafe cleanup | Closed locally: repository lint passes with zero findings | Keep full lint required in CI; remove deprecated exports only after replacement parity |
| R-014 | Medium | Giant component creates regression and AI-context risk | `App.tsx` remains large and legacy views still exist behind a bridge; the RIVT Daily Home check-in, daily engagement loop, shared UI primitives, Tools primitive alignment, Shop Talk command center, Shop Talk reaction/social pulse, Shop Talk answer queue, Trade News real-media/mobile-layout, Tools app surface, Heavy 16th multi-mode, Invoice Draft app upgrade, Daily Log tool, Daily Log Records bridge, and Daily Log live UI proof passes reduce repeated shell/Home/Crew/Inbox/Profile/Work/Tools/Records/Shop Talk/Trade News UI code but do not finish the strangler migration. | Continue strangler migration by domain; no wholesale rewrite |
| R-015 | Medium | Navigation contradicts approved IA | Mitigated in Packet 03 production and preserved through the Packet 08 UI system, shared UI primitives, Tools primitive alignment, Shop Talk command center, Trade News real-media/mobile-layout, RIVT Daily Home check-in, Shop Talk answer queue, iPhone SE containment, physical small-phone compact-device guard, and immersive-tool compact-device containment passes: primary shell is Home, Work, Crew, Shop Talk, Tools; messages/search/notifications/profile are top-bar actions; desktop/mobile E2E and rendered UI QA cover the shell, Home, Shop Talk/Trade News, and SE-sized tool surfaces; the app now forcibly prefers the mobile shell on SE-class coarse-pointer devices even when viewport reporting is odd, and immersive tools can reclaim the tiny-phone viewport instead of rendering under shell chrome. | Keep legacy bridge from reintroducing old More/role-toggle patterns; finish one physical iPhone Safari/PWA verification pass before broad launch |
| R-016 | Medium | "Verified/insured/OSHA cert" copy can overclaim | Seed talent boolean claims removed from current data; Packet 07 production changes current shell copy to evidence-state and safety-module language. The 2026-07-03 minimal truthfulness pass removed the hardcoded profile `Verified` badge/chip and stopped deriving identity/payment readiness claims from the generic consent flag. The 2026-07-04 launch-QA cleanup changes safety quiz completion copy to local-device persistence language until server-owned safety records exist. Legacy/deferred surfaces and legal review still need launch signoff. | Evidence-state vocabulary and counsel/content review |
| R-017 | High | Backup exists without verified backup-artifact restore evidence | Mitigated: encrypted cloud-only snapshot reconciliation, timed isolated logical restore, and named backup-artifact restore all passed. On 2026-06-21 `npm run backup:logical-artifact` created encrypted object `backups/postgres/2026-06-21T04-14-48.795Z-332dbc0.json.gz.aes256gcm` from 59 tables / 1,524 rows in 630 ms. `npm run restore:logical-artifact -- --apply-migrations` restored that named artifact to isolated Railway target `Postgres-_FQz`, applied nine migrations through `0009_durable_rate_limits`, restored 59 tables / 1,524 rows, verified strict manifest parity with zero diffs in 13,411 ms, and `npm run restore:drill` verified the target in 1,862 ms. The temporary target was deleted, detached restore volumes were marked for deletion, and temporary restore variables were removed. `docs/operations/recovery-policy.json` and `npm run launch:readiness` now make the remaining RPO/RTO, retention, cadence, and recovery-approval gaps machine-checkable. | Record founder-approved RPO/RTO policy, retention window, recurring restore-drill cadence, next due date, and recovery-policy approvals before Gate A approval |
| R-018 | Medium | Direct undeclared dependency may disappear | Closed and CI-proven: `fast-xml-parser` is declared and installed from lockfile | Verify deployed build |
| R-019 | High | Match acceptance source could differ from production behavior without DB-backed/live evidence | Closed for Packet 04: GitHub Actions run `27862175954` passed disposable PostgreSQL coverage; production runs migration `0005_match_acceptance`; live smoke `packet04-20260620061146-411fdf` verified application, offer, mutual acceptance, exactly-one active work, participant creation, private-address release, wrong-recipient rejection, and active-work timeline actions | Preserve Packet 04 smoke coverage and extend the same evidence standard to messaging/records/reviews |
| R-020 | High | Messaging could appear sent/read in UI without server persistence or participant authorization | Closed for Packet 05: live smoke `packet05-20260620123233-891897` proved accepted-work-only conversation creation, outsider denial, idempotent message persistence, unread persistence through relogin, private-address-safe notifications, mute suppression, report persistence, block enforcement, and cleanup | Preserve Packet 05 smoke coverage and extend the same evidence standard to reviews and admin/support |
| R-021 | High | Review/admin/safety implementation could pass source checks but fail against live PostgreSQL policy | Closed for Packet 07 and rechecked in Packet 08: production readiness confirmed migration `0008_reviews_admin_safety`, live smoke `packet07-20260620143318-94c6bd` passed review/dispute/admin/safety/support/restriction/block flows, and Packet 08 hardening audit passed on exact source `bf42ee6`. | Preserve Packet 07 smoke coverage and include review/admin/safety in future release hardening |
| R-022 | High | Operators may miss incidents because logs are not routed to alerts | Packet 08 added structured JSON request/domain logs and operational controls. A scheduled GitHub Actions production synthetic check now runs `npm run monitor:production` every 30 minutes against public health/provider/fail-closed routes, uploads monitor evidence, opens/updates one GitHub incident issue on failure, and closes it after recovery. `docs/operations/incident-routing.json`, `docs/operations/INCIDENT_REHEARSAL_RUNBOOK.md`, and `npm run incident:readiness` now make owner, support-hours, error-monitoring, paging, rehearsal, and approval gaps machine-checkable; the current check is blocked with only primary owner and synthetic monitoring configured. | Configure dedicated error monitoring and paging, name a backup owner, record support hours and approvals, rehearse an incident, then pass `npm run incident:readiness -- --require-ready` and `npm run launch:readiness -- --require-ready` before Gate A launch |
| R-023 | Medium | Tools could imply production billing, SMS delivery, tax automation, or official project records before those systems are accepted | Packet 08 Tools studio deploy, Tools primitive alignment pass, Tools app surface pass, Heavy 16th multi-mode pass, Invoice Draft app upgrade, Daily Engagement Loop, fraction-calculator simplification/ergonomics, and immersive small-phone containment keep invoice drafting local/browser-only with local browser templates/drafts, copy/download/print, plus `mailto:`/`sms:` device draft actions and explicit copy that server email/text delivery is not production-ready from local tools. The Daily Log Records bridge can write a server-backed private project note only when an accepted active-work record exists; otherwise Daily Log remains a clearly labeled device-local draft with copy/download/local-save fallback. The exact Daily Log UI path passed split live smoke `daily-log-ui-20260622004926-05a797` against real production auth, accepted work, and Records APIs with one verified timeline entry and cleanup. Calculator enhancements stay local and do not create fake project/payment/tax records, and the newest compact-device pass ensures those local tools remain usable on SE-class phones instead of rendering under shell chrome. The 2026-06-27 truthful-entitlement cleanup removed frontend-only Pro unlock from the checkout return path, disables browser-local Pro state unless `VITE_ALLOW_LOCAL_PRO=true`, documents that flag as local-development only, and shows fail-closed billing setup copy when checkout/server entitlements are unavailable. The 2026-07-03 minimal truthfulness pass removed visible Tools dev-speak such as `No fake sending` and `Server when linked`, simplified launch cards, and unified mileage/tax-summary deduction math on one shared estimate constant. The 2026-07-03 Tools hub consolidation exposed all launchable supporting utilities through compact Money/Site/Business groups and added an explicit device/cloud storage boundary line, while preserving local-only limits for standalone drafts. The 2026-07-03 tool-records persistence slice adds authenticated `tool_records` storage and wires Payment Tracker invoices/paid-state/deletes to server sync while retaining clear device-only fallback copy. The 2026-07-03 money-tools sync slice reuses that API for expenses, mileage trips, and time sessions, including one-time local snapshot upload when an account has no server records and honest device-only fallback copy when sync is unavailable. The 2026-07-03 remaining tool-records sync slice wires invoice templates, saved bids, price book entries, safety check signoffs, punch list items, daily reports, job checklist progress, and client contacts through the same authenticated `tool_records` API with one-time local snapshot upload and honest device-only fallback copy. Short-lived drafts/caches such as Daily Log local drafts, weather cache, tax-estimator scratch input, and simulated client message threads remain intentionally local/browser-only until a reviewed record type or domain API is accepted. No fake SMS, fake email, payment processing, escrow, payroll, tax export, paid-feature unlock, or official completion/closeout behavior was added. | Keep server-backed invoice sending/SMS/payment/tax exports and broader server-owned field-log workflows deferred until provider setup, authorization, delivery logs, failure recovery, canonical persistence, and live smoke evidence exist |
| R-024 | Medium | Shop Talk social actions could be mistaken for durable server reputation | Improved and deployed from commit `87923e9f34723b3fb12cf7f20a6b5b4c96e8cfb5`: migration `0017_shop_talk_reddit_backbone` adds community-owned posts, user-created communities, real member counts, server-owned answers, and server-enforced Verified Fix assignment. Migration `0018_shop_talk_moderation` adds server-owned reports, moderation statuses for communities/posts/answers, an admin report queue API, append-only moderation actions, and enforcement that hidden/locked targets are excluded or blocked from public writes. Local build/lint/security/unit/integration/e2e/audit gates passed against a Railway-backed `TEST_DATABASE_URL`; full `npm run test:integration` passed 15/15 suites; live `/api/health` reported migration `0018_shop_talk_moderation`; `EXPECTED_SOURCE_COMMIT=87923e9f34723b3fb12cf7f20a6b5b4c96e8cfb5 npm run monitor:production` passed. Branch `codex/shop-talk-moderation-console` adds the first human-facing staff console for report queues/actions and a richer Shop Talk report-reason picker with local rendered QA and full local build/lint/unit/integration/e2e/audit gates passing. | Before treating Shop Talk as broad public social reputation, merge/deploy the moderation console slice and record operational evidence that the report queue is reviewed within the committed support window |
| R-025 | Low | Some Trade News publishers may not expose usable article images | The current Trade News pass uses RSS media/enclosures and bounded public Open Graph metadata, rejects local/decorative URLs, and explicitly labels missing media instead of substituting topic art. Source outages now produce an honest empty state rather than hard-coded articles. | Monitor source/image yield and feed availability; never imply that a fallback graphic is article media or manufacture news during an outage |
| R-026 | Medium | Authenticated production click-path smoke reruns can be blocked by the shared test contractor's daily publish quota | The latest authenticated contractor production smoke passed with run `contractor-click-20260628212559-2114c1`; follow-up reruns on the same shared production contractor correctly returned `JOB_PUBLISH_LIMIT_REACHED` after 10 of 10 daily publishes. Local rendered Work lifecycle and full integration tests still pass, and production monitor passes, but same-day live publish reruns are unavailable on that actor. | Create a dedicated production test contractor per release window or wait for the quota reset before repeating authenticated live publish smoke; do not weaken the production publish rate limit for testing |
| R-027 | Medium | Weak or interrupted field connectivity can leave permanent spinners, mimic logout, reload in-progress forms, or lose unsent photos | Mitigated and deployed in source `504e1db2e5b6fc9db23883ed17a3cb7444a3a66e`: all frontend fetches are bounded, only 401 clears auth, service-worker updates require a visible-page refresh action, and failed camera/batch uploads retain their files for retry. Guest-preview and Tools smokes exercise the 5xx/401 distinction and a failed-then-successful camera upload; live `/api/health` and the production synthetic monitor passed on the exact source. | Physically test offline/stalled LTE, update activation during a form, and camera retry on iOS Safari/PWA and Android Chrome; keep the timeout/retry smoke paths required in future releases |
| R-028 | High | Notifications and accepted-work actions can claim an exact outcome while opening a generic or empty destination | Mitigated and deployed in source `e0b4fb518018989fcf8a433af5c528ff52fe7cba`: server notifications include exact job/application/active-work/project/review identifiers; the client restores those intents on cold start and browser history; the rendered Work lifecycle smoke proves offer-to-active-work and project-record navigation; and a Records self-cancellation loop found by the smoke was removed. Three affected PostgreSQL suites passed, live health served the exact source, and the production monitor passed. | Generate and tap one fresh production application/offer/project/review notification on a physical phone to close the final live acceptance boundary; retain exact-route smoke coverage in future releases |
| R-029 | High | A tool opened from accepted work can initialize against a different open job or reuse that job's draft | Mitigated and deployed in source `1f9580ca388ed2c6e28864227d58f3787165f110`: active-work responses include the job context required for tool defaults; focused tool routes wait for that record; job-aware tool components remount only on context changes; and Daily Log drafts are partitioned by active-work ID. A rendered smoke puts an unrelated job first and proves Invoice/Daily Log stay attached to the accepted job. Live health and the production monitor passed on the exact source. | Run the affected PostgreSQL integration suite when `TEST_DATABASE_URL` is available, then use a physical accepted-work account to open Invoice and Daily Log from the job workspace |
| R-030 | Medium | Notification settings can imply background delivery that no server/provider path performs | Mitigated and deployed in source `ecbd470ba9fc126eea685121030ad00f5b3b4ad0`: the browser-local subscription flag, automatic permission prompt, and foreground-only browser notification path are removed; Settings now controls the server-enforced in-app channels and states that background alerts are not connected. Railway evidence confirms Resend is configured while Twilio and VAPID are not; live health and the production monitor passed on the exact source. | Design Web Push and any general SMS use as a reviewed Gate B packet with consent, preferences, failure handling, and live delivery evidence |
| R-031 | High | A signed-out or revoked device could continue receiving private notification content | Mitigated in production source `535e21bf1c2b76c7547b9c5ac5dc9ef54b8d5b79`: push subscriptions are bound to an auth session, delivery queries require that session to remain active/unexpired, and logout/single-session/revoke-others paths remove matching endpoints. PostgreSQL integration proves another device's endpoint is deleted when its session is revoked. | Preserve session-revocation integration coverage and prove on a physical installed PWA that alerts stop after logout |
| R-032 | Medium | Push delivery can be dropped, duplicated, or become an unbounded retry loop | Mitigated in production source `535e21bf1c2b76c7547b9c5ac5dc9ef54b8d5b79` with a unique notification/subscription outbox key, row claiming with `SKIP LOCKED`, stale-claim recovery, bounded exponential retries, terminal failure state, 404/410 pruning, and delivery-row retention cleanup. Web Push provider health is configured, and physical background delivery/click-through passed on 2026-07-10. | Monitor failure logs/outbox depth and add an operator metric before broad public fan-out |
| R-033 | High | Matching-job alerts can spam the wrong trade/area, leak private jobsite data, or slow job publishing as recipient count grows | Mitigated in production by Packet 10 with active-account, selected-trade, exact public city/region/country, preference, and bidirectional-block filters; public-only payloads; idempotent source dedupe; bulk notification/outbox inserts; a live 200-recipient cap/500 hard maximum; and a fail-closed operator flag. Full local and PostgreSQL aggregate gates pass, and production health/monitoring prove the enabled controls. | Complete one controlled legitimate production publish and physical exact-job tap, then monitor publish latency and recipient counts before widening beyond Jacksonville |
| R-034 | Medium | A field photo can be captured under ambiguous job context or appear saved before the authorized upload succeeds | Packet 14 is deployed in source `dc009c799b856b45f64fda90ee22b8ff853ef4e8`: the active-job camera has a persistent exact-job label, in-camera capture type, real lens switch, one-hand shutter, and server-confirmed success. The rendered smoke forces a failed upload, retries the retained capture, then verifies it lands in the same project feed at desktop, 390px, and 320px widths; the production monitor passed. Packet 41 production source `0849eaacc0b70302bf70c487c058c33b62f99c42` adds authenticated newest-upload album covers on independent Camera while keeping accepted-work and standalone contexts isolated; exact-source health and the production monitor passed. | Take real photos into a legitimate active job and two private albums on iOS Safari/PWA and Android Chrome; preserve the failed-upload retry smoke and add a disposable-DB album-cover integration result |
| R-035 | High | Standalone field-tool use can silently inherit an unrelated marketplace job, overwrite another project's draft, or expose off-platform records across accounts | Mitigated in production source `1b38d144f83db07a305348e5e633256c666f55c2`: Camera, Estimate, and Invoice require explicit Quick/standalone/RIVT context; the first-job fallback is removed; local drafts and server records carry context; standalone project/tool-record/album access is account-owned; ambiguous dual-context writes are rejected; and all 19 PostgreSQL integration suites pass with migration 0026 ready in production. | Physically test context switching plus a real camera capture on iOS and Android before closing the field-acceptance boundary |
| R-036 | Medium | Mobile navigation can expose every feature while still making the next action unclear, clipped, or repetitive | Mitigated in production source `aaf3a8701b4dceb084bdaf007a04ea2bcba74385`: Shop Talk is feed-first with compact discovery, Work and Settings use labeled mobile section selectors, duplicate active-work context is suppressed, auth provider labels remain visible, and account/invoice surfaces no longer overflow. Five rendered UI suites cover 320px/390px mobile behavior, health serves the exact source, and the production monitor passes. | Physically review iOS and Android Settings, Shop Talk, Work, and returning-user entry before broad launch |
| R-037 | Medium | An accepted-work action can change a route but leave the phone viewport at the top of Work, making the outcome appear to fail | Mitigated in production source `5e766ca22f5d20bdfa52f7ef632274fe425f2326`: Work delegates through the canonical active-work route and then scrolls/focuses the exact job detail; the lifecycle smoke asserts URL, viewport, and focus at 390px. | Verify one physical Home or Work `Open workspace` tap lands visibly on the intended active job |
| R-038 | Medium | Accepted work can present every action as equally important, obscuring the record and daily field actions behind low-frequency job controls | Mitigated in production source `596d824b34d1f6ec84e644ec2f2a20c790907279`: the accepted-work card has one record handoff, three daily actions, compact money tools, collapsed rare controls, and rendered lifecycle coverage retaining Camera's exact-job destination. | Verify the simplified active-work card on iOS and Android with an accepted job |
| R-039 | Medium | Work can bury or mislabel accepted work, making the private lifecycle feel like an invisible, closed, or still-active result of browsing | Mitigated in production source `97a9e0fc14dba95bffe2a9297c517ba80be6b03a`: active work uses its own lifecycle instead of the closed public-listing status, distinguishes contractor and tradesperson responsibilities, exposes the participant-authorized jobsite address, keeps completion in the exact Work workspace, makes the tradesperson summary the only required closeout input, shows the contractor that exact submission, and opens submitted reviews by id. Home now selects only canonical `active` work; completed and cancelled records remain in history and cannot fall back into `You're active now`. Rendered two-role and focused Home regression coverage pass, exact-source health is green, and the production monitor passed. | Physically repeat the repaired completion and review path with one contractor and one tradesperson account; retain exact-route, status-label, and outsider-denial coverage |
| R-040 | Medium | Eight equal job-detail tabs can hide the daily work path and make rare records appear as routine actions | Packet 19 groups job detail into Today/Job/Money/More and rendered coverage asserts that Today moves active-work controls before job summary content. | Verify the four-mode job workspace on a physical active job |
| R-041 | Medium | Repeated Home launchers and a same-weight tools dashboard can obscure the actual daily next action | Packet 20 is deployed in source `3f4ea9585ef77d3769d8507fd1fd486b03f2634d`: duplicate Home recovery/answer panels are removed, one role-aware action remains, and tool utilities are compact but reachable. Build, lint, unit, E2E, mobile-action, Tools UI, health, and production-monitor gates passed. | Physically confirm Home and Tools remain easier to scan without hiding a needed action |
| R-042 | High | An Estimate Send control could claim delivery despite an unsaved draft, another account's record, provider failure, duplicate retry, or internal pricing exposure | Mitigated in deployed source `4793b1b3dd240fb709e88c820d649ea5f31f5031`: account-owned snapshot validation, saved sent/failed state, client/provider idempotency, a customer-safe itemized email, and PostgreSQL cases for validation/replay/cross-account denial. Build, lint, security lint, unit/E2E/UI gates, dependency audit, all 19 isolated PostgreSQL integration suites, live health, and the production monitor pass. Founder-controlled inbox delivery was confirmed on 2026-07-14. | Monitor live delivery errors; retain automated provider-failure and replay coverage without inducing a production provider outage |
| R-043 | Medium | Field forms can expose advanced details, history, and creation controls simultaneously, increasing missed entries and accidental actions on phones | Mitigated in production source `af13e52232ec0199a35d11f7f059942fed25b7a5`: Daily Log, Punch List, and Safety use progressive task stages, optional details remain collapsed, and rendered desktop/mobile/compact QA plus all 19 PostgreSQL suites pass without changing record contracts. | Complete a physical-phone Daily Log, Punch, and Safety cycle; preserve progressive-flow UI coverage when these records change |
| R-044 | Medium | The account drawer can duplicate Profile and Settings, making preference ownership unclear and placing low-frequency controls ahead of navigation | Mitigated in production source `657aa80eda8c1ea1de0771dc5918d2fcd0511193`: the drawer now contains identity, Profile, Settings, conditional Admin, and Sign out; duplicate metrics, device, standing, setup, and appearance surfaces are removed; mobile smoke verifies exact Settings handoffs and containment; exact-source health and the production monitor passed. | Physically confirm Profile, Settings, Admin visibility, and Sign out on one staff and one ordinary account; preserve the compact-drawer smoke in future account changes |
| R-045 | High | A negotiable listing, profile rate, applicant proposal, and accepted offer could be mistaken for the same binding amount | Mitigated in production by Packet 55: compensation mode, optional target, proposal, final offer, and accepted agreed pay are separate server-owned fields; rate-card visibility is enforced by context; positive complete amount/unit pairs are required; UI copy describes rates as references; affected migration/domain/UI suites pass; and exact source plus migration 0028 are live. | Physically complete fixed, hourly, open-to-offers, and request-quotes flows with two accounts; confirm final agreed pay on accepted work and preserve the no-payment-processing boundary |
| R-046 | Medium | A Home paperwork shortcut could open a blank or unrelated estimate/invoice, duplicate the draft on save, or silently change the user's preferred tool destination | Mitigated locally in Packet 69: queue entries carry the exact saved record, Estimate and Invoice preserve its local id, and standalone context is restored for the current open without overwriting the preferred destination. Rendered mobile and Tools UI coverage pass. | Verify one accepted-work draft and one standalone-project draft on a physical phone; run the PostgreSQL integration aggregate when `TEST_DATABASE_URL` is available |

Risk closure requires evidence and owner. Lowering severity without evidence is not closure.
